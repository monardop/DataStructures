#ifndef DYNAMICQUEUE_H
#define DYNAMICQUEUE_H

    #include <stdlib.h>
    #include "../../Utilities/utilities.h"

    typedef struct
    {
        tNode *head;
        tNode *rear;
    }dsQueue;

#endif